require 'spec_helper'

describe Statements do
  it 'has a version number' do
    expect(Statements::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
